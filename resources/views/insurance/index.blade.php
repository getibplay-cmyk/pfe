<x-app-layout>
    <div class="space-y-7">
        <x-page-header title="Assurance" eyebrow="Suivi administratif" description="Les références sensibles restent chiffrées et masquées. Toute décision de sinistre est humaine.">
            <x-slot:actions>@if(auth()->user()->hasPermission('insurance.manage'))<a href="{{ route('insurance.policies.create') }}" class="rounded-lg border bg-white px-4 py-2 text-sm">Nouvelle police</a>@endif @if(auth()->user()->hasPermission('claim.manage'))<a href="{{ route('insurance.claims.create') }}" class="rounded-lg bg-slate-900 px-4 py-2 text-sm text-white">Déclarer un sinistre</a>@endif</x-slot:actions>
        </x-page-header>
        @if(auth()->user()->hasPermission('insurance.manage'))
            <form method="POST" action="{{ route('insurance.companies.store') }}" class="grid gap-3 rounded-xl bg-white p-5 shadow-sm md:grid-cols-4">@csrf
                <label class="text-sm">Compagnie *<input name="name" required value="{{ old('name') }}" class="mt-1 w-full"><x-input-error :messages="$errors->get('name')" /></label>
                <label class="text-sm">E-mail<input name="email" type="email" value="{{ old('email') }}" class="mt-1 w-full"><x-input-error :messages="$errors->get('email')" /></label>
                <label class="text-sm">Téléphone<input name="phone" value="{{ old('phone') }}" class="mt-1 w-full"><x-input-error :messages="$errors->get('phone')" /></label>
                <div class="self-end"><button type="submit" class="rounded-lg bg-slate-900 px-4 py-2 text-white">Ajouter la compagnie</button></div>
            </form>
        @endif
        <section class="rounded-xl bg-white p-6 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-3"><h2 class="font-semibold">Polices et garanties</h2><x-result-count :paginator="$policies" /></div><div class="mt-4 overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-slate-500"><th class="p-3">Police</th><th class="p-3">Véhicule</th><th class="p-3">Compagnie</th><th class="p-3">Échéance</th><th class="p-3">Statut</th><th class="p-3"><span class="sr-only">Actions</span></th></tr></thead><tbody>@forelse($policies as $policy)<tr class="border-t"><td class="p-3 font-medium">{{ $policy->maskedPolicyNumber() }}</td><td class="p-3">{{ $policy->vehicle->registration_number }}</td><td class="p-3">{{ $policy->company->name }}</td><td class="p-3">{{ App\Support\Ui\UiLabel::date($policy->ends_at) }}</td><td class="p-3"><x-status-badge :value="$policy->status" /></td><td class="p-3 text-right"><a href="{{ route('insurance.policies.show', $policy) }}" class="text-indigo-700">Ouvrir</a></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">Aucune police.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $policies->links() }}</div></section>
        <section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Sinistres récents</h2><div class="mt-4 space-y-3 text-sm">@forelse($claims as $claim)<a href="{{ route('insurance.claims.show', $claim) }}" class="flex flex-wrap items-center justify-between gap-2 rounded-lg border p-4 hover:bg-slate-50"><span><strong>{{ $claim->claim_number }}</strong> · {{ $claim->policy->maskedPolicyNumber() }}</span><x-status-badge :value="$claim->status" /></a>@empty<x-empty-state title="Aucun sinistre" />@endforelse</div></section>
    </div>
</x-app-layout>
