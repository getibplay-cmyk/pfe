<x-app-layout>
    <div class="mx-auto max-w-4xl space-y-6"><div><p class="text-sm text-slate-500">Administration tenant</p><h1 class="text-3xl font-bold">Paramètres de l’entreprise</h1></div>
        <form method="POST" action="{{ route('tenant.update') }}" class="rounded-xl bg-white p-6 shadow-sm">@csrf @method('PATCH')<div class="grid gap-5 md:grid-cols-2"><label class="text-sm">Nom commercial<input name="name" required value="{{ old('name', $tenant->name) }}" class="mt-1 w-full rounded border-slate-300"></label><label class="text-sm">Raison sociale<input name="legal_name" value="{{ old('legal_name', $tenant->legal_name) }}" class="mt-1 w-full rounded border-slate-300"></label><label class="text-sm">E-mail<input type="email" name="email" value="{{ old('email', $tenant->email) }}" class="mt-1 w-full rounded border-slate-300"></label><label class="text-sm">Téléphone<input name="phone" value="{{ old('phone', $tenant->phone) }}" class="mt-1 w-full rounded border-slate-300"></label><label class="text-sm md:col-span-2">Adresse<textarea name="address" class="mt-1 w-full rounded border-slate-300">{{ old('address', $tenant->settings['address'] ?? '') }}</textarea></label><label class="text-sm">Devise par défaut<input name="currency" required maxlength="3" value="{{ old('currency', $tenant->settings['currency'] ?? 'MAD') }}" class="mt-1 w-full rounded border-slate-300"></label><label class="text-sm">Timezone<input name="timezone" required value="{{ old('timezone', $tenant->settings['timezone'] ?? 'Africa/Casablanca') }}" class="mt-1 w-full rounded border-slate-300"></label></div><p class="mt-4 text-xs text-slate-500">L’état plateforme et le slug ne sont pas modifiables depuis l’espace tenant.</p>@if($errors->any())<div class="mt-4 rounded bg-red-50 p-3 text-sm text-red-800">{{ $errors->first() }}</div>@endif @can('update', $tenant)<button class="mt-5 rounded bg-slate-950 px-5 py-2 text-white">Enregistrer</button>@else<p class="mt-5 text-sm text-slate-500">Consultation uniquement.</p>@endcan</form>
    </div>
</x-app-layout>
