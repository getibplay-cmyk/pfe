<x-app-layout><div class="mx-auto max-w-6xl space-y-6"><div class="flex justify-between"><div><p class="text-slate-500">{{ $vehicle->category->name }}</p><h1 class="text-3xl font-bold">{{ $vehicle->registration_number }} · {{ $vehicle->brand }} {{ $vehicle->model }}</h1></div><a href="{{ route('vehicles.edit',$vehicle) }}" class="text-blue-700">Modifier</a></div><div class="grid gap-6 lg:grid-cols-2"><section class="rounded-xl bg-white p-6"><h2 class="font-bold">Statut opérationnel</h2><p class="my-3 text-xl">{{ $vehicle->operational_status->value }}</p><form method="POST" action="{{ route('vehicles.status',$vehicle) }}">@csrf<select name="operational_status">@foreach(App\Enums\VehicleOperationalStatus::cases() as $status)<option value="{{ $status->value }}">{{ $status->value }}</option>@endforeach</select><input name="reason" placeholder="Motif"><button class="rounded bg-slate-900 px-3 py-2 text-white">Changer</button></form><ul class="mt-4 text-sm">@foreach($vehicle->statusHistories as $history)<li>{{ $history->created_at }} · {{ $history->from_status?->value ?? 'création' }} → {{ $history->to_status->value }}</li>@endforeach</ul></section><section class="rounded-xl bg-white p-6"><h2 class="font-bold">Documents privés</h2><form class="my-4 space-y-2" method="POST" enctype="multipart/form-data" action="{{ route('vehicles.documents.store',$vehicle) }}">@csrf<input name="title" placeholder="Titre" required><select name="document_type"><option value="vehicle_registration">Carte grise</option><option value="vehicle_insurance">Assurance</option><option value="vehicle_photo">Photo</option><option value="other">Autre</option></select><input type="hidden" name="is_sensitive" value="0"><input type="file" name="file" required><button class="rounded bg-slate-900 px-3 py-2 text-white">Ajouter</button></form>@forelse($vehicle->documents as $document)<a class="block text-blue-700" href="{{ route('documents.show',$document) }}">{{ $document->title }}</a>@empty<p class="text-slate-500">Aucun document.</p>@endforelse</section></div></div></x-app-layout>
