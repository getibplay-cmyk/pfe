<x-app-layout><div class="mx-auto max-w-7xl space-y-6"><div class="flex justify-between"><div><h1 class="text-3xl font-bold">Véhicules</h1><a class="text-sm text-blue-700" href="{{ route('vehicle-categories.index') }}">Gérer les catégories</a></div>@can('create', App\Models\Vehicle::class)<a class="rounded bg-slate-950 px-4 py-2 text-white" href="{{ route('vehicles.create') }}">Nouveau véhicule</a>@endcan</div><form class="grid gap-3 rounded-xl bg-white p-4 sm:grid-cols-4" method="GET"><select name="agency_id"><option value="">Toutes les agences</option>@foreach($agencies as $agency)<option value="{{ $agency->id }}" @selected(request('agency_id')==$agency->id)>{{ $agency->name }}</option>@endforeach</select><select name="category_id"><option value="">Toutes catégories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id')==$category->id)>{{ $category->name }}</option>@endforeach</select><select name="status"><option value="">Tous statuts</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->value }}</option>@endforeach</select><button class="rounded bg-slate-800 text-white">Filtrer</button></form><div class="rounded-xl bg-white shadow"><table class="w-full text-left text-sm"><thead><tr><th class="p-4">Immatriculation</th><th>Véhicule</th><th>Agence</th><th>Statut</th></tr></thead><tbody>@forelse($vehicles as $vehicle)<tr class="border-t"><td class="p-4"><a class="font-semibold text-blue-700" href="{{ route('vehicles.show', $vehicle) }}">{{ $vehicle->registration_number }}</a></td><td>{{ $vehicle->brand }} {{ $vehicle->model }}</td><td>{{ $vehicle->agency->name }}</td><td>{{ $vehicle->operational_status->value }}</td></tr>@empty<tr><td class="p-8 text-slate-500" colspan="4">Aucun véhicule.</td></tr>@endforelse</tbody></table></div>{{ $vehicles->links() }}</div></x-app-layout>
