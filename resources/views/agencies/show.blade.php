<x-app-layout>
    <div class="mx-auto max-w-6xl space-y-6"><div class="flex justify-between gap-3"><div><p class="text-sm text-slate-500">Agence {{ $agency->code }}</p><h1 class="text-3xl font-bold">{{ $agency->name }}</h1><p class="mt-1 text-sm">{{ $agency->is_active ? 'Active' : 'Inactive' }}</p></div>@can('update', $agency)<a href="{{ route('agencies.edit', $agency) }}" class="rounded border bg-white px-4 py-2 text-sm">Modifier</a>@endcan</div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach($counts as $label => $value)<section class="rounded-xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-2xl font-bold">{{ $value }}</p></section>@endforeach</div><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Utilisateurs rattachés</h2><div class="mt-4 overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-slate-500"><th class="py-2">Nom</th><th>Rôle</th><th>État</th><th>Dernière activité</th></tr></thead><tbody>@forelse($users as $user)<tr class="border-t"><td class="py-3">{{ $user->name }}</td><td>{{ $user->role?->name }}</td><td>{{ $user->is_active ? 'Actif' : 'Inactif' }}</td><td>{{ $user->last_login_at?->format('d/m/Y H:i') ?? 'Jamais connecté' }}</td></tr>@empty<tr><td colspan="4" class="py-6 text-slate-500">Aucun utilisateur rattaché.</td></tr>@endforelse</tbody></table></div></section></div>
</x-app-layout>
