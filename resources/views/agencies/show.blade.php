<x-app-layout><div class="mx-auto max-w-6xl space-y-6"><x-page-header :title="$agency->name" :eyebrow="'Agence '.$agency->code"><x-slot:actions>@can('update', $agency)<a href="{{ route('agencies.edit', $agency) }}" class="rounded-lg border bg-white px-4 py-2 text-sm">Modifier</a>@endcan</x-slot:actions></x-page-header><p><x-status-badge :value="$agency->is_active ? 'active' : 'inactive'" /></p><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach($counts as $label => $value)<x-stat-card :label="$label" :value="$value" />@endforeach</div><section class="rounded-xl bg-white p-6 shadow-sm"><h2 class="font-semibold">Utilisateurs rattachés</h2><div class="mt-4 overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-slate-500"><th class="py-2">Nom</th><th>Rôle</th><th>État</th><th>Dernière activité</th></tr></thead><tbody>@forelse($users as $user)<tr class="border-t"><td class="py-3">{{ $user->name }}</td><td>{{ App\Support\Ui\UiLabel::get($user->role?->slug) }}</td><td><x-status-badge :value="$user->is_active ? 'active' : 'inactive'" /></td><td>{{ App\Support\Ui\UiLabel::dateTime($user->last_login_at) }}</td></tr>@empty<tr><td colspan="4" class="py-6 text-slate-500">Aucun utilisateur rattaché.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $users->links() }}</div></section></div></x-app-layout>
