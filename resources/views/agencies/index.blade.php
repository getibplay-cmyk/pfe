<x-app-layout><div class="mx-auto max-w-6xl space-y-6"><x-page-header title="Agences" eyebrow="Organisation"><x-slot:actions>@can('create', App\Models\Agency::class)<a href="{{ route('agencies.create') }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white">Nouvelle agence</a>@endcan</x-slot:actions></x-page-header><form method="GET" class="grid gap-3 rounded-xl bg-white p-4 sm:grid-cols-3"><input name="q" value="{{ request('q') }}" placeholder="Nom ou code" aria-label="Recherche"><select name="status" aria-label="Statut"><option value="">Tous les statuts</option><option value="active" @selected(request('status') === 'active')>Actives</option><option value="inactive" @selected(request('status') === 'inactive')>Inactives</option></select><button type="submit" class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filtrer</button></form><x-result-count :paginator="$agencies" /><div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Code</th><th class="p-4">Nom</th><th class="p-4">Statut</th><th class="p-4"><span class="sr-only">Actions</span></th></tr></thead><tbody>@forelse($agencies as $agency)<tr class="border-t"><td class="p-4 font-medium">{{ $agency->code }}</td><td class="p-4">{{ $agency->name }}</td><td class="p-4"><x-status-badge :value="$agency->is_active ? 'active' : 'inactive'" /></td><td class="p-4 text-right"><a class="text-indigo-700" href="{{ route('agencies.show', $agency) }}">Consulter</a>@can('update', $agency)<a class="ml-3 text-blue-700" href="{{ route('agencies.edit', $agency) }}">Modifier</a>@endcan</td></tr>@empty<tr><td class="p-8 text-slate-500" colspan="4">Aucune agence ne correspond aux filtres.</td></tr>@endforelse</tbody></table></div>{{ $agencies->links() }}</div></x-app-layout>
