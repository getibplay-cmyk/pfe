<x-app-layout><div class="mx-auto max-w-3xl space-y-6"><h1 class="text-3xl font-bold">{{ $document->title }}</h1><div class="rounded-xl bg-white p-6"><p>Type : {{ $document->document_type->value }}</p><p>Document privé — aucune URL publique.</p>@can('download',$document)<a class="mt-4 inline-block rounded bg-slate-950 px-4 py-2 text-white" href="{{ route('documents.download',$document) }}">Télécharger</a>@endcan<h2 class="mt-6 font-bold">Versions</h2>@foreach($document->versions as $version)<p>v{{ $version->version_number }} · {{ $version->original_name }} · {{ $version->created_at }}</p>@endforeach<form class="mt-5" method="POST" enctype="multipart/form-data" action="{{ route('documents.versions.store',$document) }}">@csrf<input type="file" name="file" required><button>Ajouter une version</button></form></div></div></x-app-layout>
