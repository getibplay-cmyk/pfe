<x-app-layout>
    <div class="mx-auto max-w-7xl space-y-6">
        <div class="flex flex-wrap items-center justify-between gap-3"><div><p class="text-sm text-slate-500">Administration SaaS</p><h1 class="text-3xl font-bold">Tenants</h1></div><a href="{{ route('platform.tenants.create') }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white">Nouveau tenant</a></div>
        <form class="grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ request('q') }}" placeholder="Nom, slug ou raison sociale" class="rounded-lg border-slate-300"><select name="status" class="rounded-lg border-slate-300"><option value="">Tous les états</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->value }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filtrer</button></form>
        <div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Tenant</th><th>Raison sociale</th><th>État</th><th>Créé le</th><th></th></tr></thead><tbody>@forelse($tenants as $tenant)<tr class="border-t"><td class="p-4"><strong>{{ $tenant->name }}</strong><br><span class="text-slate-500">{{ $tenant->slug }}</span></td><td>{{ $tenant->legal_name ?? '—' }}</td><td>{{ $tenant->status->value }}</td><td>{{ $tenant->created_at->format('d/m/Y') }}</td><td class="pr-4 text-right"><a href="{{ route('platform.tenants.show', $tenant) }}" class="text-indigo-700">Consulter</a></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">Aucun tenant ne correspond aux filtres.</td></tr>@endforelse</tbody></table></div>{{ $tenants->links() }}
    </div>
</x-app-layout>
