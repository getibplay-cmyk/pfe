<x-app-layout><div class="mx-auto max-w-7xl space-y-6"><x-page-header title="Tenants" eyebrow="Administration SaaS"><x-slot:actions><a href="{{ route('platform.tenants.create') }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white">Nouveau tenant</a></x-slot:actions></x-page-header><form method="GET" class="grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><input name="q" value="{{ request('q') }}" placeholder="Nom, slug ou raison sociale" aria-label="Recherche"><select name="status" aria-label="État"><option value="">Tous les états</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ App\Support\Ui\UiLabel::get($status) }}</option>@endforeach</select><button type="submit" class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filtrer</button></form><x-result-count :paginator="$tenants" /><div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Tenant</th><th class="p-4">Raison sociale</th><th class="p-4">État</th><th class="p-4">Créé le</th><th class="p-4"><span class="sr-only">Actions</span></th></tr></thead><tbody>@forelse($tenants as $tenant)<tr class="border-t"><td class="p-4"><strong>{{ $tenant->name }}</strong><br><span class="text-slate-500">{{ $tenant->slug }}</span></td><td class="p-4">{{ $tenant->legal_name ?? '—' }}</td><td class="p-4"><x-status-badge :value="$tenant->status" /></td><td class="p-4">{{ App\Support\Ui\UiLabel::date($tenant->created_at) }}</td><td class="p-4 text-right"><a href="{{ route('platform.tenants.show', $tenant) }}" class="text-indigo-700">Consulter</a></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">Aucun tenant ne correspond aux filtres.</td></tr>@endforelse</tbody></table></div>{{ $tenants->links() }}</div></x-app-layout>
