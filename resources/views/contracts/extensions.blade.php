<x-app-layout><div class="mx-auto max-w-6xl space-y-6">
    <x-page-header :title="__('Demandes de prolongation')" :description="__('Examinez les demandes des locataires. Les dates ne changent qu’après leur acceptation de l’avenant.')" />
    <x-section-card :title="__('Demandes à suivre')"><x-responsive-table><table class="rf-table"><thead><tr><th>{{ __('Contrat') }}</th><th>{{ __('Véhicule') }}</th><th>{{ __('Retour demandé') }}</th><th>{{ __('État') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>
        @forelse($extensions as $extension)<tr><td><a class="rf-button-link" href="{{ route('contracts.show', $extension->rental_contract_id) }}">{{ $extension->rentalContract->contract_number }}</a></td><td>{{ $extension->rentalContract->vehicle->registration_number }}</td><td>{{ App\Support\Ui\UiLabel::dateTime($extension->requested_return_at) }}</td><td>{{ $extension->label() }}</td><td>@if(auth()->user()->hasPermission('contract.version') && auth()->user()->hasPermission('contract.accept') && auth()->user()->hasPermission('document.upload'))<a class="rf-button-link" href="{{ route('contract-extensions.show', $extension) }}">{{ __('Examiner') }}</a>@endif</td></tr>@empty<tr><td colspan="5"><x-empty-state :title="__('Aucune demande de prolongation')" /></td></tr>@endforelse
    </tbody></table></x-responsive-table><div class="mt-4">{{ $extensions->links() }}</div></x-section-card>
</div></x-app-layout>
