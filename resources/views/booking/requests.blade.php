<x-app-layout><div class="mx-auto max-w-6xl space-y-6">
    <x-page-header :title="__('Demandes de réservation publiques')" :description="__('Transformez les demandes retenues en brouillons, puis vérifiez les dossiers avant confirmation.')"><x-slot:actions>@if(auth()->user()->isTenantOwner())<a class="rf-button-secondary" href="{{ route('booking-admin.settings') }}">{{ __('Configurer le catalogue') }}</a>@endif</x-slot:actions></x-page-header>
    <x-responsive-table><table class="rf-table"><thead><tr><th>{{ __('Véhicule') }}</th><th>{{ __('Départ') }}</th><th>{{ __('Retour') }}</th><th>{{ __('État') }}</th><th>{{ __('Action') }}</th></tr></thead><tbody>@forelse($bookings as $booking)<tr><td>{{ $booking->vehicle->registration_number }}</td><td>{{ App\Support\Ui\UiLabel::dateTime($booking->starts_at) }}</td><td>{{ App\Support\Ui\UiLabel::dateTime($booking->ends_at) }}</td><td>{{ __(match($booking->status) { 'pending' => __('À examiner'), 'converted' => __('Brouillon créé'), default => __('Classée sans suite') }) }}</td><td><a class="rf-button-link" href="{{ route('booking-admin.show', $booking) }}">{{ __('Examiner') }}</a></td></tr>@empty<tr><td colspan="5"><x-empty-state :title="__('Aucune demande reçue')" /></td></tr>@endforelse</tbody></table><x-slot:footer>{{ $bookings->links() }}</x-slot:footer></x-responsive-table>
</div></x-app-layout>
