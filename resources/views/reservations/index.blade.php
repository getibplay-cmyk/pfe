<x-app-layout>
    <div class="mx-auto max-w-7xl space-y-6">
        <div class="flex items-center justify-between"><div><p class="text-sm text-slate-500">Booking</p><h1 class="text-2xl font-bold">Réservations</h1></div>@can('create', App\Models\Reservation::class)<a href="{{ route('reservations.create') }}" class="rounded-lg bg-slate-900 px-4 py-2 text-sm text-white">Nouvelle réservation</a>@endcan</div>
        <form class="grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Numéro RES-…" class="rounded-lg border-slate-300"><select name="agency_id" class="rounded-lg border-slate-300"><option value="">Toutes les agences</option>@foreach($agencies as $agency)<option value="{{ $agency->id }}" @selected(request('agency_id') == $agency->id)>{{ $agency->name }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300"><option value="">Tous les statuts</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filtrer</button></form>
        <div class="overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-3">Numéro</th><th class="p-3">Client</th><th class="p-3">Agence</th><th class="p-3">Période</th><th class="p-3">Véhicule</th><th class="p-3">Statut</th><th class="p-3">Total</th></tr></thead><tbody>@forelse($reservations as $reservation)<tr class="border-t"><td class="p-3"><a class="font-medium text-indigo-700" href="{{ route('reservations.show', $reservation) }}">{{ $reservation->reservation_number }}</a></td><td class="p-3">{{ $reservation->customer->displayName() }}</td><td class="p-3">{{ $reservation->agency->name }}</td><td class="p-3">{{ $reservation->starts_at->timezone(config('reservations.display_timezone'))->format('d/m/Y H:i') }}<br><span class="text-slate-400">au {{ $reservation->ends_at->timezone(config('reservations.display_timezone'))->format('d/m/Y H:i') }}</span></td><td class="p-3">{{ $reservation->vehicle?->registration_number ?? 'À affecter' }}</td><td class="p-3">{{ $reservation->status->label() }}</td><td class="p-3">{{ $reservation->total_amount }} {{ $reservation->currency }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-slate-500">Aucune réservation.</td></tr>@endforelse</tbody></table></div>{{ $reservations->links() }}
    </div>
</x-app-layout>
