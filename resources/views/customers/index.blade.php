<x-app-layout><div class="mx-auto max-w-6xl space-y-6"><x-page-header title="Clients" eyebrow="Relations"><x-slot:actions>@can('create',App\Models\Customer::class)<a class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white" href="{{ route('customers.create') }}">Nouveau client</a>@endcan</x-slot:actions></x-page-header><form method="GET" class="flex gap-3 rounded-xl bg-white p-4"><label class="sr-only" for="customer-search">Rechercher</label><input id="customer-search" name="q" value="{{ request('q') }}" placeholder="Nom ou société" class="min-w-0 flex-1"><button type="submit" class="rounded-lg bg-slate-900 px-4 py-2 text-white">Rechercher</button></form><x-result-count :paginator="$customers" /><div class="overflow-x-auto rounded-xl bg-white"><table class="min-w-full text-left text-sm"><thead><tr><th class="p-4">Client</th><th class="p-4">Contact</th><th class="p-4">Identité</th></tr></thead><tbody>@forelse($customers as $customer)<tr class="border-t"><td class="p-4"><a class="text-blue-700" href="{{ route('customers.show',$customer) }}">{{ $customer->displayName() }}</a></td><td class="p-4">{{ $customer->email ?? $customer->phone ?? '—' }}</td><td class="p-4">{{ $protector->maskEncrypted($customer->identity_number_encrypted) ?? 'Non renseignée' }}</td></tr>@empty<tr><td colspan="3" class="p-8 text-slate-500">Aucun client ne correspond à la recherche.</td></tr>@endforelse</tbody></table></div>{{ $customers->links() }}</div></x-app-layout>
