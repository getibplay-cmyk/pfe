<x-app-layout><div class="mx-auto max-w-6xl space-y-6"><div class="flex justify-between"><h1 class="text-3xl font-bold">Clients</h1>@can('create',App\Models\Customer::class)<a class="rounded bg-slate-950 px-4 py-2 text-white" href="{{ route('customers.create') }}">Nouveau client</a>@endcan</div><form method="GET"><input name="q" value="{{ request('q') }}" placeholder="Rechercher"><button>Rechercher</button></form><div class="rounded-xl bg-white"><table class="w-full text-left text-sm"><thead><tr><th class="p-4">Client</th><th>Contact</th><th>Identité</th></tr></thead><tbody>@forelse($customers as $customer)<tr class="border-t"><td class="p-4"><a class="text-blue-700" href="{{ route('customers.show',$customer) }}">{{ $customer->displayName() }}</a></td><td>{{ $customer->email ?? $customer->phone }}</td><td>{{ $protector->maskEncrypted($customer->identity_number_encrypted) ?? 'Non renseignée' }}</td></tr>@empty<tr><td colspan="3" class="p-8 text-slate-500">Aucun client.</td></tr>@endforelse</tbody></table></div>{{ $customers->links() }}</div></x-app-layout>
