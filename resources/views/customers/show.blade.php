<x-app-layout><div class="mx-auto max-w-6xl space-y-6"><div class="flex justify-between"><h1 class="text-3xl font-bold">{{ $customer->displayName() }}</h1><a href="{{ route('customers.edit',$customer) }}" class="text-blue-700">Modifier</a></div><section class="rounded-xl bg-white p-6"><p>Identité : {{ $protector->maskEncrypted($customer->identity_number_encrypted) ?? 'Non renseignée' }}</p>@can('viewIdentity',$customer)<a class="text-blue-700" href="{{ route('customers.identity',$customer) }}">Consulter la valeur complète (audité)</a>@endcan</section><div class="grid gap-6 lg:grid-cols-2"><section class="rounded-xl bg-white p-6"><h2 class="font-bold">Conducteurs</h2>@foreach($customer->drivers as $driver)<p class="mt-3">{{ $driver->first_name }} {{ $driver->last_name }} · @if($driver->isLicenceExpired())<span class="text-red-700">Permis expiré</span>@elseif($driver->isLicenceExpiringSoon())<span class="text-amber-700">Expire bientôt</span>@else<span class="text-emerald-700">Valide</span>@endif</p>@endforeach<form class="mt-5 grid gap-2" method="POST" action="{{ route('customers.drivers.store',$customer) }}">@csrf<input name="first_name" placeholder="Prénom" required><input name="last_name" placeholder="Nom" required><input name="licence_number" placeholder="Numéro permis" required><input type="date" name="licence_expires_at" required><input type="hidden" name="verification_status" value="pending"><input type="hidden" name="is_primary" value="0"><button class="rounded bg-slate-900 px-3 py-2 text-white">Ajouter conducteur</button></form></section><section class="rounded-xl bg-white p-6"><h2 class="font-bold">Documents privés</h2><form class="my-4 grid gap-2" method="POST" enctype="multipart/form-data" action="{{ route('customers.documents.store',$customer) }}">@csrf<input name="title" placeholder="Titre" required><select name="document_type"><option value="customer_identity">Identité</option><option value="other">Autre</option></select><input type="hidden" name="is_sensitive" value="1"><input type="file" name="file" required><button class="rounded bg-slate-900 px-3 py-2 text-white">Ajouter</button></form>@forelse($customer->documents as $document)<a class="block text-blue-700" href="{{ route('documents.show',$document) }}">{{ $document->title }}</a>@empty<p class="text-slate-500">Aucun document.</p>@endforelse</section></div></div></x-app-layout>
