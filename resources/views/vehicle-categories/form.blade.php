<x-app-layout><form class="mx-auto max-w-3xl space-y-4 rounded-xl bg-white p-6" method="POST" action="{{ $category->exists ? route('vehicle-categories.update', $category) : route('vehicle-categories.store') }}">@csrf @if($category->exists)@method('PUT')@endif<h1 class="text-2xl font-bold">{{ $category->exists ? 'Modifier' : 'Créer' }} une catégorie</h1><div class="grid gap-4 sm:grid-cols-2">@foreach(['code'=>'Code','name'=>'Nom','acriss_code'=>'Code ACRISS','seats'=>'Places','doors'=>'Portes','luggage_capacity'=>'Bagages'] as $name=>$label)<label>{{ $label }}<input class="mt-1 w-full rounded border-slate-300" name="{{ $name }}" value="{{ old($name, $category->$name) }}"></label>@endforeach</div><label class="flex gap-2"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $category->is_active ?? true))> Active</label>@if($errors->any())<p class="text-red-700">{{ $errors->first() }}</p>@endif<button class="rounded bg-slate-950 px-4 py-2 text-white">Enregistrer</button></form></x-app-layout>
